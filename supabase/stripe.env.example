# PLACEHOLDERS ONLY. Configure real values in Supabase Edge Function Secrets.
# If using --env-file, keep the filled file OUTSIDE this repository with mode 0600.
STRIPE_SECRET_KEY=REPLACE_WITH_STRIPE_TEST_SECRET_KEY
STRIPE_WEBHOOK_SECRET=REPLACE_WITH_THIS_ENDPOINT_SIGNING_SECRET
APP_SITE_URL=https://your-test-site.example
STRIPE_TEST_CHECKOUT_ENABLED=false
# No frontend Stripe key is needed for hosted Checkout redirects.
